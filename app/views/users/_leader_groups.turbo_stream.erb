<turbo-stream action="replace" target="user-groups-<%= user.id %>">
  <template>
    <%= user.groups.each do |group| %>
      <%= link_to group.name, invite_to_group_path(user_id: user.id, group_id: group.id), class: "group-link" %>
    <% end %>
  </template>
</turbo-stream>
